CLUMSY_ICON ICON "clumsy-icon.ico"  /* the first icon will also defines the executable icon in Explorer */

/****************************************************/
/* The iupPreviewDlg dialog template that used to live here was only needed by
   IupFileDlg (SHOWPREVIEW=Yes). Phase 2 removed IUP, so it is gone.
   The icon above is kept: clumsy.exe still gets it in Explorer. */

/****************************************************/
/* The following lines will enable Windows XP/Vista Visual Styles */

1 24 "clumsy64.manifest"

/****************************************************/
